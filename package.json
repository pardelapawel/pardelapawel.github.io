{
  "name": "C&P wedding",
  "version": "3.2.0",
  "watch": {
    "build": {
      "patterns": [
        "i18n/index.html"
      ],
      "verbose": true,
      "delay": 2500,
      "ignore": [
        "build"
      ]
    }
  },
  "scripts": {
    "2build": "echo foo",
    "build": "(node ./addPhotos.js index.in.html index.html --id timeline --picturesdir i18n/public && tailwindcss -i ./css/main.css -o ./i18n/main.css && static-i18n --fileFormat yml -l en -i en -i pl -i ko . --files index.html -o i18n)",
    "start": "nodemon --exec 'npm run build' --watch package.json --watch css/main.css --watch index.in.html --watch locales/en.yml --watch locales/pl.yml --watch locales/ko.yml --watch addPhotos.js"
  },
  "devDependencies": {
    "tailwindcss": "^3.3.1",
    "yargs": "^17.7.1"
  },
  "dependencies": {
    "npm-watch": "^0.11.0",
    "watch": "^1.0.2"
  }
}
